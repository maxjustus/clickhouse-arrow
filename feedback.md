2. should we add some json coersion functionality with config that mirrors clickhouse's input format/output format settings and possibly extends them?
   - e.g. `to_json` and `from_json` functions to convert between JSON strings and ClickHouse data types


